apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: lab16-sehs
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: crc-csi-hostpath-provisioner
  resources:
    requests:
      storage: 5Gi
[ladmin@localhost K8S]$ cat db-data-pv.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-pv
  namespace: lab16-sehs
spec:
  capacity:
    storage: 5Gi
  storageClassName: standard
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/var/mysql"
